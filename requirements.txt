# AURA Mars — Python Dependencies
# Install: pip install -r requirements.txt

# LLM & Embeddings
ollama>=0.4.0

# Vector Database
chromadb>=0.5.0

# Data Handling & Scoring
pandas>=2.2.0
numpy>=1.26.0

# PDF Extraction
pymupdf>=1.24.0

# UI
streamlit>=1.38.0

# Charts & Visualization
plotly>=5.22.0

# HTTP Requests (for future API integration)
requests>=2.32.0

# .env file support (src/config.py loads it for all overridable settings)
python-dotenv>=1.0.0

# Similarity Computation (used in confidence scoring)
scikit-learn>=1.5.0

# NASA SPICE Toolkit bindings — optional real-ephemeris upgrade for the
# Journey to Mars / Mission Planner tabs (src/spice_ephemeris.py). The app
# works fully without it (falls back to validated analytical orbit math);
# this just makes the accuracy upgrade a zero-extra-install away. Kernel
# data files are downloaded separately and optionally via
# scripts/download_spice_kernels.py.
spiceypy>=6.0.0

# Testing
pytest>=8.2.0
